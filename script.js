function updateLight(current) {
    let newLight = ''

    if (current === "red") {
        return newLight = "green"
    }
    if ( current === "green") {
        return newLight = "yellow"
    }
    if (current === "yellow") {
        return newLight = "red"
    }

}
